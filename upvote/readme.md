# Upvote App com Vue 3

## Dia 1

-   Vue.createApp() é usado para criar uma instância de uma aplicação Vue, que é usada para registrar dados globais que serão usados pelos componentes da aplicação

```javascript
Vue.createApp({
    data() {
        return {
            item: {},
        };
    },
});
```
